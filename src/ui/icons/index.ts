/**
 * @motion/icons — index
 *
 * This file exports the shared types only.
 * Individual icon components are NOT re-exported here; import the concrete
 * icon file you need so the app does not pull in the full catalog.
 *
 * Usage:
 *   import { ArrowRightIcon } from '@ui/icons/icons/arrow-right'
 */

export type { IconProps, IconComponent } from './types'
